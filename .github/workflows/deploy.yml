name: Build and Deploy Blazor WebAssembly to GitHub Pages

on:
  push:
    branches:
      - main  # Or your main branch
  workflow_dispatch:  # Optional: lets you run it manually

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'  # or '9.0.x' if using .NET 9

      - name: Restore dependencies
        run: dotnet restore

      - name: Publish Blazor WebAssembly
        run: dotnet publish -c Release -o release

      - name: Copy index.html to 404.html (for GitHub Pages SPA support)
        run: cp release/wwwroot/index.html release/wwwroot/404.html

      - name: Deploy to GitHub Pages branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: release/wwwroot
          publish_branch: gh-pages  # <- creates or updates this branch
